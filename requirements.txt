# Mobile ALOHA × Isaac Sim 开发笔记 — 文档站构建依赖
#
# 安装:
#   pip install -r requirements.txt
#
# 或使用虚拟环境:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

mkdocs>=1.6.1
mkdocs-material>=9.5.40
pymdown-extensions>=10.11

# 中文搜索分词(MkDocs Material 内置 search 插件已支持 zh,
# 这里再装一个 jieba 以提升中文长句的分词召回率)
jieba>=0.42.1
